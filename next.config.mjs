/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['i.imgur.com', 'cloudfront.slrlounge.com', 'techsmith.com', 'www.techsmith.com', 'encrypted-tbn0.gstatic.com'],
  },
};

export default nextConfig;
